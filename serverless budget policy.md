#databricks 

A serverless budget policy is used when creating a notebook, job, pipeline, or serving endpoint. **Not when creating a SQL warehouse nor when reading SQL in a SQL warehouse.**

Serverless budget policies consist of tags that are applied to any serverless [[databricks compute]] activity.

- [ ] will both the cluster's tag and the policy's tag be logged as custom_tags in usage table?